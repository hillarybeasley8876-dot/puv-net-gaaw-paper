# 第5章 实验与结果分析

> **章节状态**：第 5 章实验章。执行第 3.5.5 节预注册判据，报告 A/B/C/D 四方向消融与 AC 组合的完整结果，并给出 GAAW 相对固定权重对照的主结论。
>
> **数据来源纪律**：本章所有数字均可回溯至 `runs/` 目录下的落盘文件。表格与图中的每个数值都由 `scripts/_ch5_tables.py` 与 `scripts/_plot_ch5.py` 从 `docs/_cv_nn_measure.json`、`runs/<run>/summary_stats.json`、`runs/rho_trace_B2.json`、`docs/_hd_spike_table.json` 直读生成，正文不含手敲字面量。
>
> **跨机器红线**：本项目实验在两台设备上完成（本机 RTX 3090 与远端 RTX 5090）。**同一张表内不并列跨机器数字**；3090 组只与 `B002_baseline150` 比较，5090 组只与 `B002_baseline150_5090` 比较。该约束在全章每一处比较中生效，并在表头显式标注机器。
>
> **判据不可事后修改**：第 3.5.5 节预注册的主指标（$\mathrm{cv}_{\mathrm{nn}}$）、接受方向（相对基线下降）、判定门槛（$2\mathrm{SE}$）与 `REJECT_NULL` 纪律在本章原样执行。本章报告的全部结果，包括对本文方法不利的结果，均按该判据裁定，不因方向不利而省略、不重设门槛、不以"趋势向好"替代未达门槛的判定。

## 5.1 实验设置

### 5.1.1 数据集与评价协议

全部实验在 PU1K 数据集上完成，patch 规模 $N = 256$、上采样倍率 $r = 4$，与第 3.4.1 节一致。数据集总量 69000 个 patch，验证区间为索引 $[65550, 69000)$，占比 5%。训练期不使用测试集。

评价指标沿用第 3.4.3 节确立的实现与口径：CD 与 HD 采用平方距离约定，各 patch 独立归一化；NUC 为本文的简化实现，第 3.4.4 节已实证其与文献报告的 uniformity 绝对值不可直接对表，故本章的 NUC 仅用于同机器同实现内部的相对比较。

$\mathrm{cv}_{\mathrm{nn}}$ 的测量口径固定为：`seed = 20260811`、`n_sample = 200`、`val_ratio = 0.05`，由 `scripts/measure_cv_nn.py` 逐字复用第 3 章 `ch3_diagnose.py` 的实现。该脚本内置口径校验，复算 `B002_baseline150` 的结果与 `docs/_ch3_diag.json` 中 `nn_pred_cv.mean` 的相对偏差为 $0.000\times10^{0}$，即两处实现在数值上完全一致。

**P2F 不可测的说明。** 第 3.5.5 节的同报指标包含 P2F。本文使用的 PU1K patch 数据不含原始网格与法向量，而 P2F 的定义需要点到面的距离，因此**本项目条件下 P2F 无法计算**。该项按第 2.5.4 节的报告纪律明写为不可测并给出理由，不以其他指标替代，也不将其从预注册清单中删除。

### 5.1.2 共享超参数与单一变量约束

全部消融组共享同一组超参数：`batch_size = 64`、`epochs = 150`、`seed = 20260811`、优化器与学习率调度、梯度裁剪阈值均与第 3.4.5 节的干净基线一致。每个消融组相对其对照组**仅改动一个变量**，改动项在表 5-1 中逐行列出。

平台区统计口径为 epoch $[75, 149]$，$n = 75$，与第 3.4.5 节一致。选择后半程作为平台区的依据是第 3.4.5 节的多窗口收敛性核验，本章不重复论证。

**表 5-1 消融组配置与单一变量对照**

| 组 | 机器 | 对照组 | 唯一改动 | 改动值 | 参数量 |
|---|---|---|---|---|---|
| A1 | 3090 | `B002_baseline150` | 双向 CD 权重 | $w_{\mathrm{fwd}} = 1.078971$，$w_{\mathrm{bwd}} = 0.921029$ | 1,152,803 |
| A2 | 3090 | `B002_baseline150` | 双向 CD 权重（强化后向） | 后向占比提升至极端 | 1,152,803 |
| C1 | 3090 | `B002_baseline150` | 均匀性损失权重 | $w_{\mathrm{unif}} = 0.00191$ | 1,152,803 |
| D1 | 3090 | `B002_baseline150` | 注意力缩放开关 | `scale_qk = True` | 1,152,803 |
| AC | 3090 | `B002_baseline150` | A1 与 C1 同时开启 | 同上二者 | 1,152,803 |
| B1 | 5090 | `B002_baseline150_5090` | 对抗权重（固定） | $w_{\mathrm{adv}} = 8.27$ | 1,152,803（+判别器 255,426） |
| B2 | 5090 | `B002_baseline150_5090`（预注册）／**B1（本章主对照）** | 对抗权重（GAAW 动态） | $r_{\mathrm{target}} = 0.1$ | 1,152,803（+判别器 255,426） |

三点须明确。其一，**生成器参数量在全部组中不变**，均为 1,152,803，故任何指标变化都不来自容量增加；第 3.5.5 节第二项设计约束要求的"与同等参数量朴素扩容作对照"因此无需单独实验，其目的已由参数量恒定直接满足。其二，B 系组引入判别器，额外 255,426 参数仅在训练期使用，推理期不参与计算。其三，B2 的对照基准存在两个：预注册指定的无对抗基线，与第 4.1.2 节修订后的 B1；两者的结果在 5.2 与 5.3 节分别报告。

---

## 5.2 预注册判据的执行结果

本节按第 3.5.5 节预注册口径逐组裁定。裁定规则为：主指标 $\mathrm{cv}_{\mathrm{nn}}$ 相对同机器基线下降且幅度超过 $2\mathrm{SE}$ 记为 `ACCEPT`；上升且超过 $2\mathrm{SE}$ 记为反向显著的 `REJECT`；未超过门槛记为 `REJECT_NULL`。

**表 5-2 主指标 $\mathrm{cv}_{\mathrm{nn}}$ 的预注册裁定（分机器，门槛 $2\mathrm{SE}$）**

| 组 | 机器 | $\mathrm{cv}_{\mathrm{nn}}$ | 相对基线 | $\Delta/2\mathrm{SE}$ | 裁定 |
|---|---|---|---|---|---|
| `B002_baseline150` | 3090 | 0.226812 | — | — | 基线 |
| A1 | 3090 | 0.237164 | $+4.56\%$ | $+2.46$ | REJECT（反向显著） |
| A2 | 3090 | 0.563454 | $+148.42\%$ | $+89.95$ | REJECT（反向显著） |
| **C1** | 3090 | **0.214478** | $\mathbf{-5.44\%}$ | $\mathbf{-3.15}$ | **ACCEPT** |
| D1 | 3090 | 0.454851 | $+100.54\%$ | $+44.04$ | REJECT（反向显著） |
| AC | 3090 | 0.243689 | $+7.44\%$ | $+4.23$ | REJECT（反向显著） |
| `B002_baseline150_5090` | 5090 | 0.223049 | — | — | 基线 |
| B1 | 5090 | 0.256190 | $+14.86\%$ | $+7.95$ | REJECT（反向显著） |
| B2 | 5090 | 0.239709 | $+7.47\%$ | $+3.84$ | REJECT（反向显著） |

图 5-2 以图形方式给出同一裁定结果，其中门槛线为 $-1 \times 2\mathrm{SE}$。

**七组中仅 C1 通过预注册的 `ACCEPT` 判定。** 这一结果须原样陈述，不作修饰。以下逐组说明。

### 5.2.1 A 方向：双向 Chamfer 非对称加权

A1 将双向 CD 的前向与后向权重由等权改为 $1.078971 : 0.921029$，其设计依据是第 3.5.1 节实测的后向分量持续占优（训练全程 150 个 epoch 无一次低于 0.5，推理期 77.0% 样本后向占优）。设计意图是通过降低后向权重来纠正该不对称性。

实测结果为 $\mathrm{cv}_{\mathrm{nn}}$ 上升 $4.56\%$（$+2.46 \times 2\mathrm{SE}$），CD 亦由 0.002085 升至 0.002119。**该设计在预注册主指标上反向显著，判定为 REJECT。**

这一结果对第 3.5.1 节的机制解释构成反证。第 3.5.1 节曾指出后向分量占优的可能原因是"覆盖项比贴合项更难优化"，并将其纠正效果的判定留给本节。现在的实测表明：**降低后向权重不仅未改善均匀性，反而使其恶化**。一个与实测方向一致的解释是，后向分量占优并非需要纠正的失衡，而是任务本身的合理配比——降低后向权重削弱了对真值覆盖的监督，使输出点更倾向聚集，从而抬高了 $\mathrm{cv}_{\mathrm{nn}}$。该解释与实测方向一致，但**未经针对该机制的受控实验验证，本文不将其作为定论**。

A2 将后向权重推向极端。其 $\mathrm{cv}_{\mathrm{nn}}$ 达到 0.563454（$+148.42\%$），CD 升至 0.003944，而 **NUC 达到 37.920837，为基线 0.569607 的 66.6 倍**。三项指标同时大幅恶化，且 NUC 的量级跳变符合第 3.4.4 节标定的"均匀性完全退化"特征。据此判定 **A2 为训练失败而非性能较差的有效配置**，其数值不参与任何趋势性讨论，在图 5-4 的权衡分析中整组剔除并注明理由。

A2 的价值在于给出了加权范围的上界证据：**双向 CD 权重的调整存在使训练崩溃的区间**，因此该方向若要继续，必须先确定安全区间，而这不属于本文范围。

### 5.2.2 C 方向：均匀性损失

C1 引入 PU-GAN 的均匀性损失{{cite:PUGAN}}，权重 $w_{\mathrm{unif}} = 0.00191$。该取值照搬原文，本文未作调优，符合第 3.5.5 节第三项"不得依赖对超参数单独调优"的约束。

实测 $\mathrm{cv}_{\mathrm{nn}}$ 由 0.226812 降至 0.214478，下降 $5.44\%$（$-3.15 \times 2\mathrm{SE}$），**超过预注册门槛，判定为 ACCEPT。这是七组中唯一通过主指标判定的配置。**

同报指标呈现明确的方向分歧：CD 由 0.002085 升至 0.002305（$+10.6\%$），HD 由 0.008791 降至 0.008651，NUC 由 0.569607 降至 0.550877。即**均匀性改善的同时保真度下降**。按第 2.5.4 节"指标反向变化是方法真实性质"的原则，该分歧如实报告，不作取舍。

分层指标方面，Q4/Q1 比值由 1.4760 升至 1.7038。第 3.5.5 节预设"若改进针对稀疏区聚合，则该比值应下降"。**实测比值上升，说明 C1 的作用机制不是改善稀疏区聚合**，而是在全局尺度上压低间距离散度，其代价集中在输入稀疏的区域。这一判断由预注册的分层指标直接支持，属于预注册机制检验起到实际作用的一例。

### 5.2.3 D 方向：注意力缩放

D1 开启 SC-MSA 的 $1/\sqrt{d}$ 注意力缩放。**该项在第 3.2.3 节已明确声明不属于本文方法贡献**，设为开关的目的是消除该实现细节作为混杂因子。

实测 $\mathrm{cv}_{\mathrm{nn}}$ 由 0.226812 升至 0.454851（$+100.54\%$，$+44.04 \times 2\mathrm{SE}$），CD 由 0.002085 升至 0.002239。NUC 为 0.572016，与基线 0.569607 接近，故 D1 未出现 A2 式的完全退化，但主指标恶化一倍。

这一结果的意义是方法论的而非性能的：它表明**在本文的网络配置下，该标准做法并非无害默认项**。若在其他消融组中默认开启该缩放，其影响将掩盖被测机制的效果。因此本文其余全部组保持该开关关闭，该决定由本组实测支持。

### 5.2.4 组合：A1 与 C1 同时开启

AC 组同时启用 A1 的非对称 CD 加权与 C1 的均匀性损失。若两项改动的效果可叠加，预期 $\mathrm{cv}_{\mathrm{nn}}$ 应优于 C1 单独的 0.214478。

实测为 0.243689（$+7.44\%$，$+4.23 \times 2\mathrm{SE}$），**不仅差于 C1 单独，也差于 A1 单独的 0.237164，判定为 REJECT。** CD 为 0.002361，同为三者中最差。

**该负面结果具有正面价值。** 三组数据构成完整的对照链：A1 单独 $+4.56\%$、C1 单独 $-5.44\%$、组合 $+7.44\%$。这支持一项具体的结论：**在本组实验的损失配置下，保真度端与均匀性端的改善不能通过损失项的简单叠加同时获得。** 该结论的适用范围严格限定为本组实验涉及的两个损失项与其权重取值，**不外推至其他损失组合、其他权重取值或其他网络结构**。

AC 组的 Q4/Q1 为 1.6880，介于 A1 的 1.4388 与 C1 的 1.7038 之间，与"两项机制的分层效应部分相消"这一方向一致，但该解释同样未经受控验证。

### 5.2.5 B 方向相对无对抗基线的结果

按预注册口径，B1 与 B2 均相对 `B002_baseline150_5090` 裁定。B1 为 $+14.86\%$（$+7.95 \times 2\mathrm{SE}$），B2 为 $+7.47\%$（$+3.84 \times 2\mathrm{SE}$），**两者在主指标上均反向显著，判定为 REJECT。**

这一结果如实记录，并须与两项事实并读。第一，**引入对抗分支本身使主指标恶化**：B1 相对无对抗基线 $+14.86\%$。第二，**GAAW 使这一恶化幅度接近减半**：B2 的 $+7.47\%$ 相对 B1 的 $+14.86\%$，两者相对同一基线的差距即 5.3 节的主结论。

第 4.1.2 节据此将 GAAW 的对照基准修订为 B1。该修订的理由与边界在第 4.1.2 节已完整给出，此处不重复；须强调的是**修订不改变本节的预注册裁定结果，两者并存报告**：按预注册口径 B2 为 REJECT，按 4.1.2 节修订口径 B2 相对 B1 的结论见 5.3 节。

---

## 5.3 GAAW 与固定对抗权重的对照

本节回答第 4 章 GAAW 的核心命题：给定引入对抗分支这一前提，将固定权重替换为梯度自适应权重是否带来改善。对照组为 B1（$w_{\mathrm{adv}} = 8.27$ 固定），两组同机器（5090）、同 seed、同全部其余超参数，仅 $w_{\mathrm{adv}}$ 的赋值方式不同。

**表 5-3 B2（GAAW）相对 B1（固定权重）的对照结果（同机 5090，同 seed）**

| 指标 | B1 固定 | B2 GAAW | 相对变化 | $\Delta/2\mathrm{SE}$ | 方向 |
|---|---|---|---|---|---|
| $\mathrm{cv}_{\mathrm{nn}}$ | 0.256190 | **0.239709** | $\mathbf{-6.43\%}$ | $\mathbf{-8.48}$ | 改善 |
| CD | $0.002109 \pm 0.000074$ | $\mathbf{0.001935 \pm 0.000047}$ | $\mathbf{-8.25\%}$ | $\mathbf{-8.60}$ | 改善 |
| HD | 0.009520 | 0.009202 | $-3.34\%$ | — | 改善 |
| NUC | 0.703708 | 0.701984 | $-0.24\%$ | — | 基本持平 |
| Q4/Q1 | 1.6296 | 1.6257 | $-0.0039$ | — | 基本持平 |

图 5-1 给出前两项的柱状对照，误差棒为 $\pm 2\mathrm{SE}$。

$\mathrm{cv}_{\mathrm{nn}}$ 的 $\Delta/2\mathrm{SE}$ 由两组样本均值差的合并标准误计算，$\mathrm{SE}_{\mathrm{pool}} = \sqrt{\mathrm{SE}_1^2 + \mathrm{SE}_2^2} = 0.001944$。CD 的标准误由平台区 $n = 75$ 计算。**两项主要指标的改善幅度分别为门槛的 8.48 倍与 8.60 倍，均远超预注册的 $2\mathrm{SE}$ 门槛，且方向一致。**

### 5.3.1 假设 H1–H5 的逐条裁定

第 4.2.2 节声明了五条可验证假设，其接收条件在实验前固定。逐条裁定如下。

**H1（梯度范数动态的实证存在性）。** 接收条件为 $\hat\rho_{\max} / \hat\rho_{\min} \geq 100$。实测 $\hat\rho$ 的跨 epoch 极差为 $3.92 \times 10^{6}$ 倍（最小 0.005763，最大 $2.261 \times 10^{4}$），**远超接收条件，H1 成立**。数据源 `runs/rho_trace_B2.json`，口径 caveat 见下文。

**H2（相对 B1 的 CD 改善）。** 接收条件为 $\Delta\mathrm{CD} \leq -2\mathrm{SE}$。实测 $\Delta\mathrm{CD} = -8.25\%$，$-8.60 \times 2\mathrm{SE}$，**H2 成立**。

**H3（相对 B1 的 $\mathrm{cv}_{\mathrm{nn}}$ 改善）。** 接收条件为 $\Delta\mathrm{cv}_{\mathrm{nn}} \leq -2\mathrm{SE}$。实测 $-6.43\%$，$-8.48 \times 2\mathrm{SE}$，**H3 成立**。

**H4（机制可解释性）。** 接收条件为 B1 的固定权重偏离 B2 实际值一个量级以上的 epoch 占比 $\geq 70\%$。实测 B1 对应的 $\rho_{\mathrm{fixed}} = 0.012092$ 在 B2 的 150 个 epoch 中有 **91.3%** 偏离，**H4 成立**。

**H5（相对无基线的诚实报告）。** 该项为执行性条件，要求据实报告 B2 相对无对抗基线的四项指标而不省略反向变化。5.2.5 节已报告 $\mathrm{cv}_{\mathrm{nn}}$ 为 $+7.47\%$ 的反向显著结果，**H5 履行**。

**五条假设全部满足其各自的接收条件。** 须重申第 4.2.2 节的判定规则：H1 与 H4 验证机制存在，H2 与 H3 验证机制相对 B1 有效，H5 验证对照纪律。**H1、H4 的成立不蕴涵 H2、H3 成立**，本节四项独立成立系实测结果，非逻辑推导。

### 5.3.2 机制证据：$\hat\rho$ 的时间演化

图 4-1 已给出 $\hat\rho$ 与 $\bar w_{\mathrm{auto}}$ 的 150 epoch 演化曲线。本节复述其关键读数并说明其与 5.3 节结论的关系。

**口径 caveat（须与图注同时出现）**：$\bar w_{\mathrm{auto}}$ 为 epoch 内各 batch 的算术平均；$\hat\rho = r_{\mathrm{target}} / \bar w_{\mathrm{auto}}$ 是基于该均值的代表值，**不等于 $\mathrm{mean}(\rho)$**。该量仅可用于数量级演化趋势的刻画，**不可作单 batch 精确值引用**。此外，梯度范数本身未落盘，$\hat\rho$ 由 `history.json` 中的 `train_adv_w_adaptive` 字段反解得到；反解过程与校验见 `scripts/analyze_rho_trace.py`。

三项读数如下。第一，$\hat\rho$ 全程跨越 $3.92 \times 10^{6}$ 倍，平台区 $[75, 149]$（$n = 75$）中位数为 2212.65，10% 与 90% 分位分别为 392.00 与 13161.72。第二，B1 的固定 $w_{\mathrm{adv}} = 8.27$ 对应 $\rho_{\mathrm{fixed}} = 0.012092$，在 B2 全程 91.3% 的 epoch 中与实际梯度环境失配。第三，平台区中位 $\hat\rho$ 高于 $\rho_{\mathrm{fixed}}$ 五个数量级。

由于 $\hat\rho$ 与 $w_{\mathrm{adv}}$ 成反比，高 $\hat\rho$ 对应低 $w_{\mathrm{adv}}$。因此上述读数的直接含义是：**GAAW 在多数 epoch 给出的对抗权重远低于 B1 的固定值 8.27。** 结合 5.2.5 节"引入对抗分支使主指标恶化 $14.86\%$"这一事实，一个与全部实测方向一致的解释是：在本文配置下，PU-GAN 惯例的固定权重对本文的生成器而言过大，GAAW 通过按梯度范数比自动压低权重，减少了对抗分支对重建目标的干扰。

**该解释的边界须明确。** 它与三项独立观测一致（$\hat\rho$ 曲线、B1 相对无基线的恶化幅度、B2 相对 B1 的双指标改善），但**本文未做"人工设定一个更小的固定 $w_{\mathrm{adv}}$"的对照实验**，因此无法区分 GAAW 的收益来自"动态调整"本身还是仅来自"权重整体更小"。这是本文方法归因上的一处确切局限，在 5.5 节局限性中列明，不在本节以推测方式弥补。

### 5.3.3 训练稳定性

HD 的逐 epoch 曲线在第 3 章曾观测到偶发尖峰。本节以多口径对照判断 B2 与 B1 的训练稳定性差异，图 5-3 给出结果。

**口径选择须先行说明，因为此处的口径选择直接决定结论方向。** 若对每个 run 分别用其自身的 median 与 MAD 计算尖峰阈值（自身尺度口径），则波动小的 run 得到更低的阈值，反而被计出更多尖峰——这是判据的伪影，不是稳定性的度量。因此本节以**统一绝对阈值**为主口径：全部 run 共用由基线计算的阈值 $\theta = \mathrm{median}_{\mathrm{base}} + 3 \times 1.4826 \times \mathrm{MAD}_{\mathrm{base}} = 0.00833$。

**表 5-4 平台区 HD 波动的多口径对照（同机 5090，epoch $[75,149]$，$n = 75$）**

| run | median | MAD | 平台区最大值 | 尖峰数<br>（统一阈值） | 尖峰数<br>（自身尺度） |
|---|---|---|---|---|---|
| `B002_baseline150_5090` | 0.007038 | $2.90 \times 10^{-4}$ | 0.014974 | 3 | 3 |
| B1 固定 | 0.008230 | $7.39 \times 10^{-4}$ | 0.044302 | 35 | 6 |
| **B2 GAAW** | 0.007966 | $\mathbf{3.41 \times 10^{-4}}$ | **0.039082** | **22** | 11 |

在统一绝对阈值下，B2 的尖峰数为 22，少于 B1 的 35；B2 的 MAD 为 B1 的 46%；B2 的平台区最大值低于 B1。**三项指标一致表明 B2 的训练波动小于 B1。** 在自身尺度口径下 B2 的计数（11）反而高于 B1（6），该现象的成因正是 B2 的 MAD 更小导致其自身阈值更低，属上述伪影，不构成相反结论。

两个口径的完整数据落盘于 `docs/_hd_spike_table.json`，多口径复核脚本为 `scripts/_check_hd_spikes.py`，该脚本同时输出 `median+5MAD`、`mean+3σ`、`2×median` 三个附加口径，其中前两个方向与统一阈值一致，`2×median` 口径下 B2（3）亦优于 B1（4）。**五个口径中四个支持"B2 波动更小"，唯一相反的口径已确认为伪影。**

尖峰的形态方面，图 5-3 显示尖峰在时间轴上孤立出现、次日即回落，非周期性振荡。据此，第 3 章记录的"偶发尖峰非震荡"这一判断在 B 系两组上同样成立。

---

## 5.4 保真度与均匀性的权衡

本节综合前两节结果，给出全部消融组在保真度（CD）与均匀性（$\mathrm{cv}_{\mathrm{nn}}$）两个维度上的分布。图 5-4 给出散点图，纵轴断开以隔离 D1 的离群位置，A2 因训练失败（5.2.1 节）整组剔除。

两项观察。

**第一，两端由不同机制到达。** $\mathrm{cv}_{\mathrm{nn}}$ 的最优值来自 C1（0.214478，均匀性损失），CD 的最优值来自 B2（0.001935，GAAW）。两者不是同一配置，且各自在对方的指标上并非最优：C1 的 CD 为 0.002305（劣于其基线 0.002085），B2 的 $\mathrm{cv}_{\mathrm{nn}}$ 为 0.239709（劣于其基线 0.223049）。

**第二，两端不能由叠加同时到达。** AC 组的实测（5.2.4 节）已给出直接证据：A1 与 C1 的组合在两个维度上均差于各自单独。

据此，本章的综合结论表述为：**在本文的实验范围内，保真度与均匀性构成一组需要取舍的目标，改善其中一端的机制不改善另一端，且两端的机制不能通过损失项叠加同时获得。** 该结论的适用范围限定为本文的网络配置、损失项集合与权重取值，**不作为点云上采样任务的一般性质陈述**——第 2.1.3 节引述的文献讨论与本文实测方向一致，但本文的证据不足以支撑推广到该范围之外。

---

## 5.5 局限性

按第 2.5.4 节的报告纪律，本节列明本章结论的确切边界。以下各项均为已知局限，不以其他结果弥补。

**其一，GAAW 的收益无法区分"动态"与"更小"。** 如 5.3.2 节所述，本文未设置"人工固定一个更小 $w_{\mathrm{adv}}$"的对照组，因此 B2 相对 B1 的改善中，来自梯度自适应机制的部分与仅来自权重整体更小的部分不可分离。该对照实验的设计是明确的后续工作。

**其二，主指标上 GAAW 未优于无对抗基线。** 按预注册判据，B2 相对 `B002_baseline150_5090` 在 $\mathrm{cv}_{\mathrm{nn}}$ 上为 $+7.47\%$ 的反向显著（5.2.5 节）。本章的 GAAW 结论严格限定为"相对固定权重对照的改善"，**不主张 GAAW 使系统整体优于不使用对抗分支的配置**。

**其三，P2F 不可测。** 如 5.1.1 节所述，本项目 PU1K patch 数据不含网格与法向量，P2F 无法计算。因此预注册同报指标中的这一项缺失，本文不以 HD 或 NUC 替代其功能。

**其四，未报告随机种子稳健性。** 全部结果基于单一 seed（20260811）。跨 seed 的波动幅度未测量，因此本章的 $2\mathrm{SE}$ 门槛均为样本内标准误口径，**不含 run 间随机性**。跨 epoch 平台区的 $\sigma$ 与跨 seed 的 SE 是两个不同口径，本章未混用，亦未以前者代替后者。

**其五，NUC 为简化实现。** 第 3.4.4 节已实证本文 NUC 实现与文献 uniformity 绝对值不可对表，故本章的 NUC 仅用于同机器同实现内部的相对比较，不与任何文献报告值并列。

**其六，未与文献方法横向对比。** 本章全部比较均为本项目内部的受控消融。PU1K 存在 random 与 uniform(FPS) 两种采样协议，其结果不可混用（`docs/SOTA_SURVEY.md` 已记录该风险），在协议对齐工作完成前，本文不给出与文献报告数字的对比表。

**其七，A2 与 D1 的失败机制未深究。** 两组均出现主指标大幅恶化，A2 并伴随 NUC 量级跳变。本文将其作为参数区间的边界证据使用，未对其失败机制作进一步归因。

---

## 5.6 本章小结

本章执行了第 3.5.5 节的预注册判据，报告七组消融的完整结果。

在预注册口径下，**七组中仅 C1（均匀性损失）通过 `ACCEPT` 判定**（$\mathrm{cv}_{\mathrm{nn}}$ $-5.44\%$，$-3.15 \times 2\mathrm{SE}$），其代价是 CD 上升 $10.6\%$ 且 Q4/Q1 比值由 1.4760 升至 1.7038，即其作用机制不是改善稀疏区聚合。A1、A2、D1、AC、B1、B2 六组在主指标上均为反向显著的 `REJECT`，其中 A2 经 NUC 量级判定为训练失败（37.92 对基线 0.57），D1 的恶化证实了 $1/\sqrt{d}$ 缩放在本文配置下并非无害默认项。

在第 4.1.2 节修订后的对照口径下，**GAAW（B2）相对固定权重对照（B1）在两项主要指标上同时改善且幅度一致**：$\mathrm{cv}_{\mathrm{nn}}$ $-6.43\%$（$-8.48 \times 2\mathrm{SE}$）、CD $-8.25\%$（$-8.60 \times 2\mathrm{SE}$），HD 亦改善 $3.34\%$。第 4.2.2 节预先声明的五条假设 H1–H5 全部满足其接收条件，其中机制证据为 $\hat\rho$ 跨 150 epoch 演化 $3.92 \times 10^{6}$ 倍、B1 固定值在 91.3% 的 epoch 中与实际梯度环境失配。训练稳定性方面，在统一绝对阈值口径下 B2 的 HD 尖峰数（22）少于 B1（35），MAD 为 B1 的 46%，五个口径中四个支持该方向。

综合两组结果，本章给出的权衡结论是：**保真度端与均匀性端由不同机制到达，且不能通过损失项叠加同时获得**——C1 达到均匀性端、B2 达到保真度端，而两者的组合（AC 组的直接类比）在两端均劣于各自单独。该结论的适用范围限定于本文的网络配置与损失项集合。

本章同时列明七项局限，其中最直接影响方法归因的是：**GAAW 的收益尚不能区分来自"梯度自适应"还是来自"权重整体更小"**，该对照实验为明确的后续工作；以及**GAAW 在主指标上未优于无对抗基线**，故本文的 GAAW 结论严格限定为相对固定权重对照的改善。

---

## 本章图表清单

| 编号 | 标题 | 类型 | 位置 | 数据源 |
|---|---|---|---|---|
| 表 5-1 | 消融组配置与单一变量对照 | 表格 | §5.1.2 | `runs/*/config.yaml` |
| 表 5-2 | 主指标 $\mathrm{cv}_{\mathrm{nn}}$ 的预注册裁定 | 表格 | §5.2 | `docs/_cv_nn_measure.json` |
| 表 5-3 | B2 相对 B1 的对照结果 | 表格 | §5.3 | `docs/_cv_nn_measure.json` + `runs/*/summary_stats.json` |
| 表 5-4 | 平台区 HD 波动的多口径对照 | 表格 | §5.3.3 | `docs/_hd_spike_table.json` |
| 图 5-1 | GAAW 与固定权重的双指标对照 | 数据图 | §5.3 | `paper_assets_TRIAL/figures_ch5/F5_1_B2_vs_B1.png` |
| 图 5-2 | 各消融组的预注册裁定 | 数据图 | §5.2 | `paper_assets_TRIAL/figures_ch5/F5_2_verdicts.png` |
| 图 5-3 | HD 逐 epoch 曲线与尖峰多口径对照 | 数据图 | §5.3.3 | `paper_assets_TRIAL/figures_ch5/F5_3_HD_per_epoch.png` |
| 图 5-4 | 保真度—均匀性权衡分布 | 数据图 | §5.4 | `paper_assets_TRIAL/figures_ch5/F5_4_tradeoff.png` |
| 图 4-1 | GAAW 训练期 $\hat\rho$ 演化（第 4 章图，本章引用） | 数据图 | §5.3.2 | `paper_assets_TRIAL/figures_ch4/F4_1_rho_curve_B2.png` |

---

**本章撰写说明**

- 全章数字由 `scripts/_ch5_tables.py` 与 `scripts/_plot_ch5.py` 从存档 json 生成，审计脚本为 `scripts/_audit_ch5.py`。
- 跨机器红线在每张表的表头显式标注机器，无跨机并列。
- 预注册判据原样执行；C1 为唯一 `ACCEPT`，其余六组据实报告为 `REJECT`。
- 5.3.3 节的口径选择说明先于结论出现，因该节结论方向依赖口径选择；五个口径的完整结果落盘可查。
- 遵守 `docs/STYLE_GUIDE.md §2.9` 断言纪律：A 类挂 `{{cite:⟨key⟩}}`；B 类标注 run 与口径；C 类（如 A1 反向结果的机制解释、GAAW 收益的成因解释）均紧邻依据陈述并显式标注未经受控验证。
- 遵守 `docs/FORMAT_TONGJI.md` 排版规范。

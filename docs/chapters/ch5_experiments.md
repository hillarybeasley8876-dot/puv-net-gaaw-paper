# 第 5 章 实验结果与分析

> **数据来源纪律**：本章所有数字均回溯至 `docs/_thesis_results.json`（全文唯一数字入口，由 `scripts/build_thesis_results.py` 从 `docs/_cv_nn_measure.json` 汇总生成）以及三张插图的 `.meta.json` 存档。正文不出现任何未落盘的数值。`{{cite:⟨key⟩}}` 占位符与前四章统一编号规则，最终在论文交付时一次替换。
>
> **跨机器红线**：本章同一张表内的数字不跨机器并列。3090 组各消融只与 `B002_baseline150` 对照，5090 组各消融只与 `B002_baseline150_5090` 对照。该约束来自 `docs/_thesis_results.json` 的 `redline` 字段，在下文每张表的表注中重申。

## 5.1 实验设置与评价口径

### 5.1.1 数据划分与采样口径

本章全部指标在同一验证子集上测得。数据集为 PU1K 的 patch 形式，共 69000 个样本；按 `val_ratio = 0.05` 取尾部区间 $[65550, 69000)$ 为验证区间，在该区间内以固定随机种子 20260811 抽取 $n = 200$ 个样本作为本章唯一的评测集合。上采样率固定 $r = 4$，推理阶段关闭数据增强，噪声强度置零。

该口径的两项核验已完成留痕。第一，抽样索引的落域核验：经 `scripts/_adjudicate_n11_claim.py` 逐条检查，9 个论文级 run 各自的 200 条样本索引全部落在验证区间内，区间外 0 条。第二，与第 3 章诊断口径的一致性核验：本章测量脚本 `scripts/measure_cv_nn.py` 逐字复用第 3 章 `ch3_diagnose.py` 的度量实现，在 `E000_metric_calibration` 上比对的相对偏差为 $0.000×10^{0}$。因此本章数字与第 3 章基线诊断可直接对照，不存在口径漂移。

### 5.1.2 三项指标的语义

本章报告三项指标，均为越小越好。

**$\mathrm{cv}_{\mathrm{nn}}$（局部间距离散度）** 刻画输出点云内部最近邻间距的变异系数，直接对应上采样任务所要求的"点分布均匀"这一目标。它是本章的主指标。

**CD（Chamfer 距离）** 刻画输出相对真值的平均几何保真度。它对整体形状偏移敏感，但对局部聚簇不敏感——两个 CD 相同的输出可以在均匀性上相差甚远，这一点在第 3 章的基线诊断中已由实测确认。

**HD（Hausdorff 距离）** 刻画最坏区域的风险，即输出中偏离真值最远的那一部分点的偏离幅度。

三项联合报告的必要性在于它们不可互相替代：仅报 CD 会掩盖均匀性缺陷，仅报 $\mathrm{cv}_{\mathrm{nn}}$ 无法排除"以牺牲保真度换均匀"的退化解。本章 5.4 节的权衡分析正建立在这一互补关系上。

### 5.1.3 显著性判据与其效力边界

本章的接收判据在实验开始前定死，记录于 `docs/_thesis_results.json` 的 `criteria` 字段，全章不作事后调整：

- $\Delta \leq -2\,\mathrm{SE}$：判 **ACCEPT**（相对对照有改善）；
- $|\Delta| < 2\,\mathrm{SE}$：判 **REJECT_NULL**，即在当前样本量下无法与零区分；
- $\Delta \geq +2\,\mathrm{SE}$：判 **REVERSE_SIGNIFICANT**（反向显著，即劣化）。

其中 $\Delta$ 一律以"实验组减对照组"计，$\mathrm{SE}$ 为两组标准误的合并值。

此处必须明确该判据的效力边界，否则本章后续所有裁定都会被读者高估。**本章的 $\mathrm{SE}$ 是跨样本标准误，不是跨随机种子标准误。** 它由 200 个验证样本的样本间波动算得，回答的问题是"在这 200 个样本上观察到的组间差异，是否可能仅由样本选择的偶然性造成"。它**不**回答"换一个随机种子重训，该差异是否仍然出现"。后者需要多种子重复实验，本文未做（理由见 5.6.2 节），因此本文全文不报告种子稳健性，也不将任何本章结论表述为"统计显著"。凡本章写"改善"，其完整含义是"在这 200 个验证样本上方向一致，且幅度超过样本波动的 2 倍"。

按此界定，$2\,\mathrm{SE}$ 这一倍数是本文自设的门槛而非领域通行标准，故其作用是**约束本文自身**：它在实验前定死，因此结果出来后无论有利与否都不再改动。5.3.2 节涉及一项未达该门槛的观察，本章按原门槛如实裁定，不作放宽。

## 5.2 消融总表

### 5.2.1 3090 组：均匀性与结构消融

表 5-1 报告 3090 机器上的 6 个 run。该批次对抗分支全程关闭，判别器不参与训练。

**表 5-1  3090 组消融结果（$n=200$，对照基线 `B002_baseline150`）**

| run | 角色 | best epoch | $\mathrm{cv}_{\mathrm{nn}}$ | 相对基线 | CD | 相对基线 | HD | 相对基线 |
|---|---|---|---|---|---|---|---|---|
| `B002_baseline150` | 基线 | 147 | 0.226812 | — | $1.1088×10^{-3}$ | — | $4.9276×10^{-3}$ | — |
| `ABL_A1_cd_balance` | 消融 | 142 | 0.237164 | $+4.56\%$ ↑ | $1.1116×10^{-3}$ | $+0.26\%$ | $5.2107×10^{-3}$ | $+5.74\%$ |
| `ABL_A2_cd_boost_bwd` | 消融 | 136 | 0.563454 | $+148.42\%$ ↑ | $1.4006×10^{-3}$ | $+26.32\%$ ↑ | $6.4821×10^{-3}$ | $+31.55\%$ ↑ |
| `ABL_C1_uniform` | 消融 | 93 | **0.214478** | $\mathbf{-5.44\%}$ ↓ | $1.1159×10^{-3}$ | $+0.64\%$ | $4.9599×10^{-3}$ | $+0.65\%$ |
| `ABL_AC_combo` | 消融 | 61 | 0.243689 | $+7.44\%$ ↑ | $1.1567×10^{-3}$ | $+4.32\%$ | $5.1618×10^{-3}$ | $+4.75\%$ |
| `ABL_D1_scale_qk` | 消融 | 84 | 0.454851 | $+100.54\%$ ↑ | $5.7179×10^{-3}$ | $+415.70\%$ ↑ | $6.4897×10^{-2}$ | $+1216.99\%$ ↑ |

表注：三项指标均越小越好，↑ 表示劣化、↓ 表示改善；箭头仅标注达到 $2\,\mathrm{SE}$ 门槛者。数据来源 `docs/_thesis_results.json` 之 `groups.3090_uniform_structure`。本表数字**不得与表 5-2 并列比较**：两表来自不同机器，绝对值不可跨机器对照。

三项观察。

**第一，$\mathrm{C1}$ 均匀性损失是本组唯一有效项。** $\mathrm{cv}_{\mathrm{nn}}$ 由 0.226812 降至 0.214478，相对变化 $-5.44\%$（改善）。其标准误为 $1.358×10^{-3}$（基线 $1.408×10^{-3}$），差值 $-1.233×10^{-2}$ 达合并标准误的多倍，判 ACCEPT。代价是 CD 与 HD 各上升不足 $0.7\%$，二者均未达 $2\,\mathrm{SE}$ 门槛，故不认定为劣化。即 C1 以几乎无保真度代价换取了均匀性改善。

**第二，A 系（双向 CD 非对称加权）方向全错。** A1 温和加权使 $\mathrm{cv}_{\mathrm{nn}}$ 反而上升 $4.56\%$；A2 激进加权将 $\mathrm{cv}_{\mathrm{nn}}$ 推高至 0.563454，为基线的 2.48 倍，同时 CD 上升 $26.32\%$、HD 上升 $31.55\%$，三项全部反向显著。这一结果的解释见 5.5.1 节。

**第三，D1 明确不构成本文创新。** 在 SC-MSA 的 $QK^\top$ 上补 $1/\sqrt{d}$ 缩放后，三项指标同时崩塌：$\mathrm{cv}_{\mathrm{nn}}$ 上升 $100.54\%$，CD 上升 $415.70\%$，HD 上升 $1216.99\%$。该项在本文中的定位是**排除性实验**：它证明原实现省略该缩放并非疏漏，而是与其余超参共同构成了一个可训练的配置；单独补上该项会破坏该配置。本文不将其列为改进点，5.5.3 节说明其解释。

### 5.2.2 5090 组：对抗赋权消融

表 5-2 报告 5090 机器上的 3 个 run。该批次启用判别器，额外引入 255426 个参数。

**表 5-2  5090 组消融结果（$n=200$，对照基线 `B002_baseline150_5090`）**

| run | 角色 | best epoch | $\mathrm{cv}_{\mathrm{nn}}$ | 相对基线 | CD | 相对基线 | HD | 相对基线 |
|---|---|---|---|---|---|---|---|---|
| `B002_baseline150_5090` | 基线 | 126 | 0.223049 | — | $1.1110×10^{-3}$ | — | $5.2517×10^{-3}$ | — |
| `ABL_B1_adv_fixed` | 消融 | 118 | 0.256190 | $+14.86\%$ ↑ | $1.1457×10^{-3}$ | $+3.13\%$ | $5.0835×10^{-3}$ | $-3.20\%$ |
| `ABL_B2_adv_adaptive` | 消融（本文方法） | 120 | 0.239709 | $+7.47\%$ ↑ | $1.1301×10^{-3}$ | $+1.73\%$ | $4.9973×10^{-3}$ | $-4.84\%$ |

表注：数据来源 `docs/_thesis_results.json` 之 `groups.5090_adversarial`。本表数字**不得与表 5-1 并列比较**。

本表须与 5.3 节合读，因为它回答的不是本文的主问题。表 5-2 的对照基线是**无对抗基线**，而本文的方法论定位是"在既有对抗训练框架内改进权重给定方式"，其对照应为固定权重的对抗训练（B1）而非无对抗训练（B0）。表 5-2 因此呈现两层事实：

其一，**引入对抗分支本身在本文实现下损害了均匀性**：B1 相对 B0 的 $\mathrm{cv}_{\mathrm{nn}}$ 上升 $14.86\%$，B2 相对 B0 上升 $7.47\%$，两者均为反向显著。这一事实按第 3.5.5 节预注册的诚实性要求如实报告，不省略、不淡化。

其二，**在已引入对抗分支的前提下，自适应赋权把这一损害减半**：B1 的 $+14.86\%$ 降至 B2 的 $+7.47\%$。这正是 5.3 节主对比所要检验的内容。

对该"减半"须给出正确读法：它**不构成"GAAW 使模型优于无对抗基线"的证据**。B2 相对 B0 的 $\mathrm{cv}_{\mathrm{nn}}$ 仍高出 $7.47\%$，方向为劣化。本文的主张严格限定为"若采用对抗训练，则梯度自适应赋权优于固定权重"，而非"对抗训练优于不用对抗训练"。后一命题在本文实测中不成立，本文不作该主张。

## 5.3 主对比：B2（GA-PUT）相对 B1（固定权重）

### 5.3.1 主结果

本节是本文对外的核心对比。B1 与 B2 同机器、同批次、同随机种子、同 batch size，唯一差异是对抗损失权重的给定方式：B1 采用固定 $w_{\mathrm{adv}} = 8.27$（该值来自 B-001 试跑的对抗权重训练期监控量均值，取值依据见 4.1.3 节），B2 采用第 4 章提出的梯度自适应赋权（GAAW）。

**表 5-3  主对比 B2（GAAW）vs B1（固定 $w_{\mathrm{adv}}=8.27$），$n=200$**

| 指标 | B1 | B2 | $\Delta$（B2−B1） | 相对变化 | 合并 SE | $|\Delta| / \mathrm{SE}$ | 裁定 |
|---|---|---|---|---|---|---|---|
| $\mathrm{cv}_{\mathrm{nn}}$ | 0.256190 | **0.239709** | $-1.6481×10^{-2}$ | $\mathbf{-6.43\%}$ | $1.9438×10^{-3}$ | $\mathbf{8.48}$ | **ACCEPT** |
| CD | $1.1457×10^{-3}$ | $1.1301×10^{-3}$ | $-1.5611×10^{-5}$ | $-1.36\%$ | $4.8585×10^{-5}$ | 0.32 | REJECT_NULL |
| HD | $5.0835×10^{-3}$ | $4.9973×10^{-3}$ | $-8.6132×10^{-5}$ | $-1.69\%$ | $2.4820×10^{-4}$ | 0.35 | REJECT_NULL |

表注：数据来源 `docs/_thesis_results.json` 之 `main_comparison`。$\mathrm{SE}$ 为跨样本标准误（$n=200$），非跨种子标准误。裁定门槛 $2\,\mathrm{SE}$ 于实验前定死。

**主结论。** 在均匀性指标 $\mathrm{cv}_{\mathrm{nn}}$ 上，GAAW 相对固定权重改善 $6.43\%$，偏移量达跨样本标准误的 8.48 倍，判 ACCEPT。该改善是本文的核心实测结果，也是 4.2.2 节 **H3 接收条件（$\Delta\mathrm{cv}_{\mathrm{nn}} \leq -2\,\mathrm{SE}$）的满足**。

该结论的稳固性来自倍数本身的量级。8.48 倍标准误意味着：即使把判据门槛提高到通行的 $1.96\,\mathrm{SE}$、或进一步提高到 $3\,\mathrm{SE}$、$5\,\mathrm{SE}$，该结论均不改变。换言之它不依赖本文所选的具体门槛值。

### 5.3.2 CD 与 HD：方向向好但不作有效性主张

在 CD 与 HD 两项上，GAAW 相对固定权重的点估计方向**同为改善**，分别为 $-1.36\%$ 与 $-1.69\%$。这一方向本身是有信息量的结论：它表明**均匀性的改善未以几何保真度或最坏区域风险的恶化为代价**，即本文方法不属于"以牺牲保真度换均匀"的退化解。此点对上采样方法尤为关键，因为强行均匀化输出的最常见失效模式正是整体形状偏移。

但两项的偏移量分别仅为跨样本标准误的 **0.32 倍**与 **0.35 倍**，远低于本文实验前设定的 2 倍标准误接收门槛，均判 REJECT_NULL。故本文**不对 CD 与 HD 作有效性主张**：在当前样本量下，二者的改善无法与零区分，其观察值与"GAAW 对 CD/HD 无影响"这一假设完全相容。

此处按原门槛裁定，理由须写明。$2\,\mathrm{SE}$ 门槛系本文自设，但正因其自设，事后放宽即构成对预注册判据的违反，其效果等同于按结果调整判据。且该门槛的松紧在此处并不影响结论：0.32 与 0.35 的倍数距任何常用门槛（$1\,\mathrm{SE}$、$1.96\,\mathrm{SE}$、$2\,\mathrm{SE}$）均有显著距离，即使采用最宽松的 $1\,\mathrm{SE}$ 亦不通过。这不是门槛过严，而是在 200 样本量下该量级的差异被样本波动完全覆盖。

作为对比，$\mathrm{cv}_{\mathrm{nn}}$ 的 8.48 倍与 CD 的 0.32 倍相差 26 倍。两者不可混为一谈，本文亦不将三项指标合并表述为"全面改善"。

**H2 的裁定。** 4.2.2 节预注册的 H2 接收条件为 $\Delta\mathrm{CD} \leq -2\,\mathrm{SE}$。实测 $|\Delta|/\mathrm{SE} = 0.32$，**H2 不成立**。按 4.2.2 节"H1–H5 逐条独立"的总判定规则，H2 的失败不否定 H1、H3、H4：H1、H4 验证机制存在（已在第 4 章满足），H3 验证机制带来均匀性改善（本节满足），三者与 H2 无逻辑蕴涵关系。本文因此保留机制论述与均匀性主张，同时明确 CD 维度的改善未获实测支持。

### 5.3.3 逐样本配对的改善占比

均值差异只回答"平均而言是否更好"，不回答"是否在多数样本上都更好"。两者可以分离：一个由少数样本的大幅改善拉动的均值，与一个在绝大多数样本上一致小幅改善的均值，在均值层面可能相同，但方法可靠性含义完全不同。本节因此报告逐样本配对的改善占比。

该量的性质须先说明：它是**事实陈述而非假设检验**。"200 个样本中有多少个方向变好"是可直接清点的计数，本文未对其设定接收门槛，也不作显著性裁定。B1 与 B2 使用同一机器、同一批次、同一组 200 个验证样本，按样本索引严格配对（索引集合完全一致已核验，脚本 `scripts/paired_improvement.py`，结果落盘 `docs/_paired_improvement_B2_vs_B1.json`）。

**表 5-4  B2（GAAW）相对 B1（固定权重）的逐样本配对改善占比（$n=200$）**

| 指标 | 改善样本数 | 变差样本数 | 改善占比 | 配对差值中位数 |
|---|---|---|---|---|
| $\mathrm{cv}_{\mathrm{nn}}$ | **158** | 42 | **79.00\%** | $-1.7194×10^{-2}$ |
| CD | 131 | 69 | 65.50\% | $-1.9463×10^{-5}$ |
| HD | 106 | 94 | 53.00\% | $-1.2686×10^{-4}$ |

表注：配对差值以 B2 减 B1 计，负值表示 B2 更优；三项指标均越小越好。无持平样本。数据来源 `docs/_paired_improvement_B2_vs_B1.json`。

三项结果的读法各不相同，须分别陈述。

**$\mathrm{cv}_{\mathrm{nn}}$：79.00\% 的样本改善**，即约每五个样本中有四个变好。该占比与 5.3.1 节均值层面的 ACCEPT 裁定方向一致，且进一步表明该改善不是由少数样本的极端增益拉动的均值假象，而是在样本总体上普遍出现。

**CD：65.50\% 的样本改善。** 此处出现一项须如实说明的现象：CD 在均值层面未达门槛（$0.32\,\mathrm{SE}$，5.3.2 节判 REJECT_NULL），但在配对计数层面呈现约 2:1 的多数。二者不矛盾，其成因是两个层面的噪声结构不同——均值比较承受了样本间固有的量级差异（不同形状的 CD 绝对值本身相差数倍），而配对计数只看同一样本上的方向，样本间差异被抵消。

**但本文不因此改判 CD 的有效性。** 5.3.2 节的裁定依据是实验前定死的均值门槛，本节的配对计数是在结果已知之后追加的分析视角。若在看到配对结果有利之后改用配对口径重新裁定，即构成按结果选择判据，其性质与事后放宽门槛相同。故本文的处理是：**CD 的有效性主张仍不作出**，配对占比仅作为描述性补充信息报告，供读者自行判断。本文亦不在此处引入配对标准误重算显著性，理由同上。

**HD：53.00\% 的样本改善**，即改善与变差样本数接近各半（106 对 94）。该结果与 5.3.2 节 HD 未达门槛（$0.35\,\mathrm{SE}$）的裁定完全一致，并为其提供了更直观的解释：HD 上的方向性差异在样本层面近乎随机，均值上的 $-1.69\%$ 主要来自个别样本的波动而非普遍趋势。本文因此明确，**HD 维度不存在可主张的改善**。

综合三项，本节的结论是：GAAW 的作用在均匀性维度上普遍且方向一致（79.00\%），在几何保真度维度上呈多数倾向但不作有效性主张（65.50\%），在最坏区域风险维度上与随机无异（53.00\%）。三者不可合并表述为"全面改善"。

**尾部质量的分位对照（补充）。** 以每样本 $\mathrm{cv}_{\mathrm{nn}}$ 的第四四分位均值与第一四分位均值之比 $Q_4/Q_1$ 衡量样本间质量落差：B1 为 1.629584，B2 为 1.625657，差异 $-0.24\%$。该量表明 GAAW 在改善均匀性的同时**既未扩大也未缩小样本间的质量落差**，即改善并非以牺牲部分样本为代价。此项同为描述性统计，本文未对其设定接收门槛，故不作显著性裁定。

## 5.4 定性与权衡分析

### 5.4.1 点云定性对比（图 5-1）

图 5-1 给出三个验证样本在四个 run 下的输出点云对比，是本文对"均匀性改善"这一数值结论的视觉验证。

**样本选取规则与其局限**（记录于 `F5_1_qualitative_pointclouds.meta.json`）：三个样本按 $\mathrm{CD}(\mathrm{B1}) - \mathrm{CD}(\mathrm{B2})$ 降序脚本选取，即按 CD 增益最大者选出，得索引 66077、67002、67133，全部落在验证区间 $[65550, 69000)$ 内。

该选取规则带来一项必须如实报告的后果：**样本按 CD 增益选出，而非按 $\mathrm{cv}_{\mathrm{nn}}$ 增益选出，因此逐样本 $\mathrm{cv}_{\mathrm{nn}}$ 并非在每个样本上都有利于 B2**。三个样本的逐 panel $\mathrm{cv}_{\mathrm{nn}}$ 实测为：

| 样本索引 | B0（无对抗） | B1（固定） | B2（GAAW） | C1（均匀性损失） |
|---|---|---|---|---|
| 66077 | 0.240398 | 0.237872 | 0.219412 | 0.185108 |
| 67002 | 0.194286 | 0.275252 | 0.229684 | 0.218974 |
| 67133 | 0.217989 | 0.252791 | **0.256054** | 0.203703 |

样本 67133 上 B2 的 $\mathrm{cv}_{\mathrm{nn}}$（0.256054）**略高于** B1（0.252791），即在该单个样本上 GAAW 的均匀性表现更差。本文保留该事实并写入图注，不通过重新选取样本掩盖。这一不一致与 5.3.1 节的总体结论不矛盾：表 5-3 的 $-6.43\%$ 是 200 样本均值，个别样本方向相反属预期内的样本波动，而这正是 5.1.3 节引入标准误判据的原因。

图 5-1 每个样本占三行。第一行为全局视图，着色为逐点到真值的前向 CD（色图 `inferno`，亮色表示误差大）；第二行为放大视图，放大窗口由脚本按"真值密集而预测覆盖不足"的准则自动选定，即在 $12×12$ 的 XY 网格中取真值计数不低于非空格中位数、且预测/真值计数比最小的格；第三行为全局视图，着色为点内最近邻间距相对真值中位间距的比值（色图 `viridis`）。

着色范围按样本共享而不跨样本共享：同一样本的四个 run 使用同一色标（上限取该样本全 run 的 99 分位），不同样本各自独立。此设定的必要性在于各样本的误差量级相差数倍，全图统一色标会使多数样本的差异被压平至不可辨。因此**图 5-1 的颜色只可在同一样本的四列之间横向比较，不可跨样本纵向比较**。

图中 C1 位于 3090 机器，B0、B1、B2 位于 5090 机器。本图不并列任何跨机器的数值指标，仅作形态对照，故不违反跨机器红线。

### 5.4.2 均匀性与保真度的权衡（图 5-3）

图 5-3 以 $\mathrm{cv}_{\mathrm{nn}}$ 为横轴、CD 为纵轴，将全部 run 置于同一权衡平面，误差棒为跨样本标准误。两台机器分置左右两个面板，各自以本组基线画虚线十字，绝对值不跨面板比较。

三项读图结论。

**C1 向左移动而纵向几乎不动**，即在保真度基本不变的前提下取得均匀性改善，是本文实测中"无代价改善"的唯一实例。

**AC_combo 落在基线的右上方**，即 $\mathrm{cv}_{\mathrm{nn}}$ 与 CD 同时劣化。A 与 C 单独作用时 C 有效、A 无效，组合后不但未继承 C 的收益，反而整体劣于基线，构成"组合倒赔"。该负面结果的分析见 5.5.2 节。

**B2 位于 B1 的左下方**，即相对固定权重在均匀性与保真度两个方向同时向好。须重申 5.3.2 节的限定：纵向（CD）的位移量为 $0.32\,\mathrm{SE}$，未达门槛，故该"左下"应读作"横向改善已确认、纵向未观察到代价"，而非两项皆有效。

两个 run 因数值过大移出坐标轴，改以白底标注并在 `.meta.json` 中落数：`ABL_A2_cd_boost_bwd` 的 $\mathrm{cv}_{\mathrm{nn}} = 0.563454$，为其余各点的约 2.4 倍；`ABL_D1_scale_qk` 的 $\mathrm{CD} = 5.7179×10^{-3}$，为其余各点的约 5 倍。此处置的目的是避免两个离群点把主群压缩成一团而丧失分辨力，同时不丢弃其数值信息。

## 5.5 负面结果与非创新项

本节独立成节，目的是使本文的负面结果获得与正面结果同等的报告完整度。

### 5.5.1 A 系失效：双向 CD 加权无法替代显式均匀性约束

A 系的设计假设是：基线的双向 CD 中反向项占比偏高（第 3 章实测 $54.46\%$），提高反向权重可迫使输出更充分地覆盖真值，从而间接改善均匀性。该假设在实测中被否定：A1 的 $\mathrm{cv}_{\mathrm{nn}}$ 上升 $4.56\%$，A2 上升 $148.42\%$，且加权越激进劣化越严重，呈单调关系。

一项可测量的解释是：A2 的 HD 逐样本离散度显著异常。其推理期逐样本 HD 的变异系数为 1.037，是其余各 run（$0.451$–$0.505$）的约 2 倍；超过中位数 2 倍的样本数为 19，超过 5 倍的样本数为 **4**，而其余 8 个 run 超过 5 倍者**均为 0**（数据来源 `F5_2_hd_epoch_curve.meta.json` 之 `inference_per_sample_hd_spread`）。即激进反向加权在部分样本上产生了严重的局部失效，而非在全体样本上均匀地略微变差。该现象与"反向 CD 主导优化后模型倾向于把点堆向真值密集区、从而在稀疏区留下大空洞"这一机制方向一致；本文将该关联作为推断提出，其直接的逐点证据未在本文实测范围内取得。

### 5.5.2 AC_combo：组合倒赔

A 与 C 的组合（`ABL_AC_combo`）$\mathrm{cv}_{\mathrm{nn}}$ 为 0.243689，相对基线上升 $7.44\%$，反向显著；CD 上升 $4.32\%$，HD 上升 $4.75\%$。而 C 单独作用时 $\mathrm{cv}_{\mathrm{nn}}$ 的相对变化为 $-5.44\%$（改善）。组合后不但未保留 C 的收益，整体表现反而劣于两者中较好的一项，亦劣于基线。

此外该 run 的 best epoch 为 **61**，为 9 个论文级 run 中最早，显著早于基线的 147 与 C1 的 93。这提示组合损失下的选择指标在训练早期即达峰随后退化，即组合并非"收益相抵"而更可能是"优化过程本身被干扰"。

本文将此列为负面结果如实报告，并严格限定其外推范围：该结论**仅适用于本文所实现的 A 与 C 两项损失在本文所用权重下的组合**，不外推至其他损失项组合、其他权重配置或其他网络结构。本文亦不声称"多损失组合普遍无效"——该断言超出本文实测范围。

### 5.5.3 D1：明确的非创新项

如 5.2.1 节所报，在 SC-MSA 的 $QK^\top$ 上补 $1/\sqrt{d}$ 缩放（`puvnet/models/pu_transformer.py:289`）导致三项指标同时崩塌：$\mathrm{cv}_{\mathrm{nn}}$ $+100.54\%$、CD $+415.70\%$、HD $+1216.99\%$。

本文对该结果的定位须精确。它**不**表明"缩放点积注意力是错误设计"——该设计在 Transformer 文献中的作用已被广泛确立。它表明的是：在本文所复现的这一具体实现中，其余超参（学习率、损失权重、初始化）是在无缩放条件下调定的，单独补上缩放会改变注意力 logits 的量级分布，使原超参配置失效。因此 D1 的正确读法是"该修改需伴随超参重调，本文未做该重调"，而非"该修改本身有害"。

本文将 D1 列为**排除性实验**而非改进点：其价值在于证明本文对基线的复现中该省略是已知的、被检验过的，而非未察觉的实现偏差。

## 5.6 HD 的两种口径与本章的报告边界

### 5.6.1 训练期与推理期 HD 不可混称

HD 在本文中存在两个不同口径的量，二者的离散度统计**不可合并**，此点在 `F5_2_hd_epoch_curve.meta.json` 中以 `caliber_warning` 字段固化。

**训练期逐 epoch $\mathrm{monitor\_hd}$**（$n = 150$，每 epoch 一个值，字段名 `monitor_hd`）。图 5-2 给出该量的逐 epoch 曲线，按机器分置两个面板。核心观察是**对抗组的训练期 HD 波动显著大于无对抗组**：变异系数为 B1 $0.397$、B2 $0.412$，而无对抗组为 B0-3090 $0.135$、B0-5090 $0.186$、C1 $0.157$，前者约为后者的 2.5 倍。最大值与中位数之比亦然：B1 为 $5.20$、B2 为 $4.67$，无对抗组为 $1.82$–$2.22$。

按"超过该 run 逐 epoch 中位数 2 倍"计尖峰，各 run 尖峰 epoch 为：B0-3090 无（0 次）、C1 第 77 epoch（1 次）、B0-5090 第 1、147 epoch（2 次）、B1 第 110、111、126、128 epoch（4 次）、B2 第 76、104、144 epoch（3 次）。

**此处的 2 倍阈值系在观察数据之后选定，因此上述尖峰计数为描述性统计，不构成假设检验结论。** 该限定同时写入图 5-2 图注。其可读出的信息限于"对抗训练在本文实现下引入了额外的训练期不稳定性，且该不稳定性在固定权重与自适应赋权下均存在"，不可读作"GAAW 减少了 HD 尖峰"——4 次与 3 次之差在 150 个 epoch 的样本上不支持该推断。

**推理期逐样本 HD**（$n = 200$，每样本一个值，字段名 `hd`）。该口径下**不存在离群样本**：全部 9 个 run 中，超过各自中位数 5 倍的样本数**均为 0**（A2 除外，为 4，已在 5.5.1 节单独讨论）。主对比三个 run 的变异系数为 B0-5090 $0.468$、B1 $0.480$、B2 $0.505$，$p_{90}/p_{10}$ 为 $2.95$、$2.91$、$3.15$，三者相近。

因此**"HD 存在巨大方差"这一表述只在训练期口径下成立，在推理期逐样本口径下不成立**。表 5-1 至表 5-3 报告的 HD 均值属推理期口径，其 $\pm\sigma$ 不应被解释为训练不稳定性的证据。本文明确区分二者，不以其中一个口径的离散度支持另一口径的结论。

### 5.6.2 本章不报告的内容及其理由

为使本章的结论边界可核查，此处显式列出本文未报告的项目及理由。

**跨随机种子稳健性：不报告。** 本文全部 9 个论文级 run 均使用单一种子（3090 组 seed 20260811，5090 组同）。因此本文无法给出任何结论的跨种子方差，5.1.3 节的 $\mathrm{SE}$ 一律为跨样本标准误。本文不以"未测"的方式为该项留出解释空间，而是明确：**凡涉及跨种子可复现性的主张，本文一概不作。** 这同时限定了第 4 章 H1 的适用范围（见 4.3.4 节 caveat）：H1 确立的是"在 `ABL_B2_adv_adaptive` 这一次 150 epoch 训练内梯度范数比非平稳"，而非该机制在任意初始化下必然呈现同一量级的演化。

**P2F（point-to-surface）指标：不可测。** 该指标需要真值网格或法向量信息，而本文所用 PU1K patch 形式数据仅含点坐标，不含 mesh 与法向量。该缺失属数据形式的固有限制，非本文实现遗漏，故本章不报告 P2F，亦不以其他指标替代后声称已覆盖表面贴合度。

**与已发表方法的横向绝对值对表：不进行。** 理由有二。其一，PU1K 的评测存在 random 与 uniform（FPS）两种采样协议，两种协议下的指标绝对值不可混用，而本文所复现基线使用的协议与各文献报告值的协议对应关系未逐篇核实。其二，本文的 NUC 类均匀性指标为简化实现，其绝对值与文献报告的 uniformity 指标不同源。在此条件下强行对表将产生不可解释的比较结论，故本文的全部比较限定在**本文内部同口径的消融对照**范围内。

**跨机器绝对值比较：不进行。** 如各表表注所述，3090 组与 5090 组的数字不并列。该约束的原因是两批次的运行环境（GPU 型号、驱动、CUDA 版本）不一致，浮点累加次序差异足以在指标第三位有效数字上产生偏移，而本文若干结论的效应量与该偏移量级可比。

## 5.7 本章小结

本章在 $n = 200$ 的同口径验证子集上报告了 9 个论文级 run 的实测结果，主要结论如下。

**其一，本文方法的核心主张获得实测支持且限定明确。** GAAW（B2）相对固定权重对抗训练（B1）在均匀性指标上改善 $6.43\%$，达跨样本标准误的 8.48 倍，H3 成立；逐样本配对进一步显示 200 个样本中 158 个（$79.00\%$）方向变好，该改善在样本总体上普遍出现而非由少数极端样本拉动。同时 CD 与 HD 的点估计方向同为改善（$-1.36\%$、$-1.69\%$），即该改善未以几何保真度或最坏区域风险为代价；但二者偏移量仅为 $0.32\,\mathrm{SE}$ 与 $0.35\,\mathrm{SE}$，低于实验前设定的门槛，故本文不对 CD 与 HD 作有效性主张，H2 不成立。CD 的配对改善占比为 $65.50\%$（呈多数倾向），HD 为 $53.00\%$（与随机无异），本文不据此改判，理由见 5.3.3 节。

**其二，对抗训练本身在本文实现下损害均匀性。** B1、B2 相对无对抗基线的 $\mathrm{cv}_{\mathrm{nn}}$ 分别上升 $14.86\%$ 与 $7.47\%$，均为反向显著。本文的主张严格限定为"若采用对抗训练，则梯度自适应赋权优于固定权重"，不主张对抗训练优于不用对抗训练。

**其三，显式均匀性约束（C1）是本文实测中代价最低的改善项。** $\mathrm{cv}_{\mathrm{nn}}$ 相对变化 $-5.44\%$（改善），CD 与 HD 的变化均未达门槛。

**其四，两项负面结果与一项非创新项如实报告。** A 系双向 CD 加权无法替代显式均匀性约束且加权越激进劣化越严重；A 与 C 的组合出现"组合倒赔"且 best epoch 提前至 61；D1 的 $1/\sqrt{d}$ 缩放在未重调超参的条件下导致三项指标崩塌，本文将其定位为排除性实验而非改进点。

**其五，本章的报告边界已显式列出。** 跨种子稳健性不报告、P2F 不可测、与已发表方法的横向绝对值不对表、跨机器绝对值不并列。本文的全部有效性结论限定在本文内部同口径消融对照的范围内。

# CompShare / UCloud 凭证 —— 本文件已被 .gitignore 屏蔽，不会进版本库
#
# 使用步骤：
#   1. 复制本文件为 .env.local  （同目录，去掉 .example）
#   2. 把下面两行的占位符换成控制台里的真实值
#   3. 不要把 .env.local 的内容粘贴到任何对话、issue、截图里
#
# 密钥获取：https://console.compshare.cn  →  账户/API 密钥
# UCloud 体系里 PrivateKey 只参与本地签名计算，不会通过网络发送。

COMPSHARE_PUBLIC_KEY=在此填入你的公钥
COMPSHARE_PRIVATE_KEY=在此填入你的私钥

# 可选：默认地域与项目，留空则用脚本内默认值
COMPSHARE_REGION=
COMPSHARE_ZONE=
COMPSHARE_PROJECT_ID=
